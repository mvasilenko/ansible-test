---
dependency:
  name: galaxy

driver:
  name: docker

lint: |
  set -e
  yamllint .
  ansible-lint

platforms:
  - name: instance-ubuntu
    image: ubuntu:lunar

provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
    verify: verify.yml
